Hướng dẫn triển khai dự án:
+ Bước 1: Clone dự án về máy tính cá nhân.
+ Bước 2: Chạy lệnh `npm install` để tải các thư viện cần thiết.
+ Bước 3: Bỏ file `.env` vào cùng cấp với file package.json và chạy lệnh `npm run dev`.
